"use client";

import React, { useState } from "react";
import { Container } from "@/components/ui/Container";
import { FadeIn } from "@/components/ui/FadeIn";
import { MessageSquare, Mail, ArrowRight, Sparkles, Database, Users } from "lucide-react";
import { cn } from "@/lib/utils";
import { motion, AnimatePresence } from "framer-motion";

const concepts = [
  { id: "lead-flow", title: "Lead capture and qualification", category: "Sales example", icon: Users, description: "A form submission becomes a structured CRM record, is checked against qualification rules, and alerts the appropriate sales owner. This is a demonstration workflow, not a client result.", pipeline: ["Lead form submission", "CRM record created", "AI qualification", "Sales notification"] },
  { id: "support-flow", title: "Customer message and handoff", category: "Support example", icon: MessageSquare, description: "A customer message is matched against approved knowledge, answered when appropriate, and routed to a team member when the request needs judgement or context.", pipeline: ["Customer message", "AI agent", "Knowledge base", "Human handoff"] },
  { id: "follow-up-flow", title: "Form submission and follow-up", category: "Operations example", icon: Mail, description: "A submitted form is stored, acknowledged by email, and placed into a follow-up sequence so the next action is visible to the team.", pipeline: ["Form submission", "Database", "Confirmation email", "Follow-up task"] },
  { id: "data-flow", title: "Inbox to structured data", category: "Data example", icon: Database, description: "Information arriving in an inbox is extracted into defined fields, checked for missing values, and made available for review or reporting.", pipeline: ["Email attachment", "Field extraction", "Validation", "Review queue"] },
];

export function WorkExamples() {
  const [activeConcept, setActiveConcept] = useState(0);
  const current = concepts[activeConcept];
  return <section id="work" className="py-24 bg-[#080a0f] border-t border-slate-800/60 relative"><Container><div className="text-center max-w-3xl mx-auto mb-16"><FadeIn direction="down" delay={0.1}><span className="text-xs font-mono uppercase tracking-widest text-cyan-400 px-3 py-1 rounded-full bg-cyan-400/10 border border-cyan-400/20 inline-block mb-4">Work examples</span></FadeIn><FadeIn direction="up" delay={0.2} preset="blur"><h2 className="text-3xl sm:text-5xl font-extrabold text-white tracking-tight mb-4">See the shape of a <span className="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">useful workflow.</span></h2></FadeIn><FadeIn direction="up" delay={0.3}><p className="text-slate-400 text-base sm:text-lg">These are illustrative blueprints to show how a process can move between people, AI, and the tools your team already uses.</p></FadeIn></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-12">{concepts.map((item, idx) => { const Icon = item.icon; const isActive = activeConcept === idx; return <motion.button key={item.id} onClick={() => setActiveConcept(idx)} whileHover={{ y: -4, scale: 1.02 }} className={cn("p-5 rounded-xl border text-left transition-all duration-300 flex flex-col justify-between group cursor-pointer", isActive ? "bg-slate-800/90 border-cyan-500/50 shadow-[0_0_20px_rgba(0,240,255,0.15)] text-white" : "bg-slate-900/40 border-slate-800 text-slate-400 hover:bg-slate-800/40 hover:text-slate-200")}><div><div className="flex items-center justify-between mb-3"><div className={cn("p-2 rounded-lg border", isActive ? "bg-cyan-400/20 border-cyan-500/40 text-cyan-400" : "bg-slate-950 border-slate-800 text-slate-500")}><Icon className="w-5 h-5" aria-hidden="true" /></div><span className="text-[10px] font-mono px-2 py-0.5 rounded bg-slate-950 text-slate-400 border border-slate-800">Demo</span></div><h3 className="text-sm font-semibold">{item.title}</h3></div><span className="text-xs text-cyan-400/80 font-mono mt-4 block">{item.category}</span></motion.button>; })}</div><FadeIn direction="up" delay={0.2}><div className="max-w-4xl mx-auto p-6 sm:p-8 rounded-2xl bg-slate-900/50 border border-slate-800 backdrop-blur-xl shadow-2xl"><AnimatePresence mode="wait"><motion.div key={activeConcept} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} transition={{ duration: 0.3 }}><div className="flex items-center justify-between gap-4 mb-4 flex-wrap"><span className="inline-flex items-center gap-1.5 text-xs font-mono uppercase px-3 py-1 rounded bg-cyan-400/10 text-cyan-400 border border-cyan-400/20"><Sparkles className="w-3.5 h-3.5" aria-hidden="true" />Illustrative blueprint</span><span className="text-xs text-slate-400 font-mono">{current.category}</span></div><h3 className="text-2xl font-bold text-white mb-3">{current.title}</h3><p className="text-slate-300 text-sm sm:text-base leading-relaxed mb-8">{current.description}</p><div className="p-6 rounded-xl bg-slate-950 border border-slate-800"><h4 className="text-xs font-mono uppercase text-slate-400 tracking-wider mb-4">Workflow sequence</h4><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">{current.pipeline.map((step, i) => <motion.div key={step} initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: i * 0.08, duration: 0.3 }} className="p-3.5 rounded-lg bg-slate-900 border border-slate-800 text-xs"><span className="text-[10px] font-mono text-cyan-400 uppercase mb-1 block">Step 0{i + 1}</span><span className="font-medium text-slate-200">{step}</span></motion.div>)}</div></div><a href="#contact" className="mt-8 block w-full py-3 px-5 rounded-lg bg-cyan-400 text-slate-950 font-semibold text-sm hover:bg-cyan-300 transition-colors text-center">Talk through a similar workflow <ArrowRight className="w-4 h-4 inline ml-1" aria-hidden="true" /></a></motion.div></AnimatePresence></div></FadeIn></Container></section>;
}
